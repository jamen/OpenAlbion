import { ListView , VerticalBox, ScrollView} from "std-widgets.slint";

export component App inherits Window {
    width: 1024px;
    height: 768px;
    ScrollView {
        width: 1024px;
        height: 768px;
        for data in [ {}, {}, {}, {}, {} ] :
        Rectangle {
            Text {
                text: "Hello";
            }
        }
    }
}