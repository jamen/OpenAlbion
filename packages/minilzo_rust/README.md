# rust-minilzo

Rust bindings for the minilzo library, a mini subset of the LZO compression library.

A copy of minilzo and LZO are found at `include/minilzo`.

The binding code was derived from [`badbod/minilzo-rs`](https://github.com/badboy/minilzo-rs).
